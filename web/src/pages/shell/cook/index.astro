---
/* Served at /recipes/:id/cook with the recipe inlined as #page-data. */
import Layout from "../../../layouts/Layout.astro"
import CookPage from "../../../islands/CookPage.svelte"
---

<Layout
  title="Cooking"
  bare
  themeColor={{ light: "#2f6b4f", dark: "#1f3d2e" }}
  data
  bodyClass="overflow-hidden"
>
  <CookPage client:only="svelte">
    <div slot="fallback" class="bg-canvas fixed inset-0">
      <div class="tile-surface h-28"></div>
    </div>
  </CookPage>
</Layout>
