[workspace]
members = ["crates/crumb-core", "crates/crumb-client", "desktop/linux"]
resolver = "3"

[package]
name = "crumb"
version = "3.0.0"
edition = "2024"
description = "Crumb: a private recipe box. Paste a link, keep just the recipe."
license = "MIT"
publish = false

[dependencies]
axum = { version = "0.8", features = ["multipart"] }
base64 = "0.22"
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
crumb-core = { path = "crates/crumb-core" }
flate2 = "1"
futures-util = { version = "0.3", default-features = false, features = ["sink", "std"] }
hmac = "0.12"
http-body-util = "0.1"
image = { version = "0.25.10", default-features = false, features = ["jpeg", "png", "webp", "gif"] }
pdf-extract = "0.7"
percent-encoding = "2"
rand = "0.8"
regex = "1"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "gzip", "brotli", "deflate", "json"] }
rusqlite = { version = "0.32", features = ["bundled"] }
scraper = "0.21"
sentry = { version = "0.49", default-features = false, features = ["backtrace", "contexts", "panic", "ureq", "rustls", "tracing"] }
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
serde_urlencoded = "0.7"
sha2 = "0.10"
subtle = "2"
tempfile = "3"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "fs", "process", "time", "signal", "sync", "io-util"] }
tokio-tungstenite = { version = "0.24", default-features = false, features = ["connect"] }
tower = { version = "0.5", features = ["util"] }
tower-http = { version = "0.6", features = ["fs", "compression-br", "compression-gzip", "normalize-path", "set-header", "trace"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
url = "2"
webp = { version = "0.3.1", default-features = false }
# Pre-release: pinned exactly. Browser TLS/HTTP2 fingerprints for recipe pages (src/scraper.rs)
wreq = { version = "=6.0.0-rc.31", features = ["gzip", "brotli", "deflate", "zstd", "cookies"] }
wreq-util = "=3.0.0-rc.14"
zip = { version = "2", default-features = false, features = ["deflate"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
panic = "unwind"

# Resizing photos in an unoptimised build is several times slower; keep the image codecs
# fast in `cargo run` too (only these crates, so debug builds stay quick)
[profile.dev.package.image]
opt-level = 3
[profile.dev.package.zune-jpeg]
opt-level = 3
[profile.dev.package.png]
opt-level = 3
[profile.dev.package.fdeflate]
opt-level = 3
[profile.dev.package.libwebp-sys]
opt-level = 3
[profile.dev.package.image-webp]
opt-level = 3
